#include <stdio.h>
/**
  * main - Entry point
  *
  * Return: Always 0 (Success)
  */

int main(void)
{
	
	int digit;

	for (digit = '0'; c <= '9'; digit++)
	{
		printf("%i",digit);
	}
	putchar('\n');
	return (0);
}
